<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="com.spring.yeoreobap.reviewReply.mapper.IReviewReplyMapper">

	<insert id="replyRegist">
		INSERT INTO reviewReply
		(review_no, review_reply, reg_date)
		VALUES (#{replyNo}, #{reviewReply}, #{regDate})
	</insert>
	
	<select id="getList" resultType="rReply">
		SELECT * 
		FROM reviewReply
		WHERE review_no = #{reviewNo}
		ORDER BY reply_no DESC
		LIMIT #{pageStart}, #{cpp}
	</select>
	
	<select id="getTotal" resultType="int">
		SELECT COUNT(*)
		FROM reviewReply
		WHERE review_no = #{reviewNo}
	</select>
	
	<!-- <select id="getContent" resultType="rReply">
		SELECT * FROM reviewReply
		WHERE review_no = #{reviewNo}
	</select> -->
	
	<select id="pwCheck" resultType="string">
		SELECT reply_pw
		FROM reviewReply
		WHERE review_no = #{reviewNo}
	</select>
	
<!-- 	<update id="update">
		update reviewReply
		SET review = #{content},
		수정됨?
		update_date = current_timestamp
		WHERE reply_no = #{replyNo}
	</update> -->
	
	<delete id="delete">
		DELETE FROM reviewReply
		WHERE reply_no = #{replyNo}
	</delete>

</mapper>