<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.spring.yeoreobap.review.mapper.IReviewMapper">

	<insert id="regist">
		INSERT INTO review
		(review_no, rest_id, writer, title, content)
		VALUES (#{reviewNo}, #{restId}, #{writer}, #{title}, #{content})
	</insert>
	<!-- regDate, updateDate 삭제 -->
	<select id="getList" resultType="review">
		SELECT * 
		FROM review
		ORDER BY review_no DESC
		LIMIT #{pageStart}, #{cpp}
	</select>
	
	<select id="getTotal" resultType="int">
		SELECT COUNT(*)
		FROM review
	</select>
	
	<select id="getArticle" resultType="review">
		SELECT * FROM review
		WHERE review_no = #{reviewNo}
	</select>
	
	<update id="update">
		UPDATE review
		SET title = #{title}, content = #{content},
		update_date = current_timestamp
		WHERE review_no = #{reviewNo}
	</update>
	
	<delete id="delete">
		DELETE FROM review
		WHERE review_no = #{reviewNo}
	</delete>
	
	

</mapper>